# The model, ingestion and evaluation live in the private gripe repository.
# Pinning a tag is the promotion mechanism: a new model reaches the site only
# when this line is bumped, and rolling back is reverting this line.
gripe @ git+ssh://git@github.com/rizbicki/gripe.git@v0.1.0
