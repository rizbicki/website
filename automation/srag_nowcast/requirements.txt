# Model, ingestion, validation and CLI live in the private gripe repository.
# Promotion and rollback happen by changing this immutable release tag.
gripe[collect] @ git+ssh://git@github.com/rizbicki/gripe.git@v0.4.0
